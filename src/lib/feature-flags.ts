// Product-wide decision: no AI-generated content in ORIZ for now.
// Code that uses it (route + UI) is kept in place, just gated off — in case
// this comes back as a paid add-on later. Flip to true to re-enable.
export const AI_CAPTION_ENABLED = false;
